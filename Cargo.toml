[workspace]
members = [
    "core",
    "bip32",
    "bitcoin",
    "litecoin",
    "psbt",
]

exclude = [
  "bitcoin-wasm",  # Exluded so we can specify optimizer flags
  "ledger",  # Excluded until we can figure out how to build hidapi on travis
]
